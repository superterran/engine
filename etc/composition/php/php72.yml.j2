
  php72: 
    build:
      context: ./
      dockerfile: ./etc/img/php72.Dockerfile
    volumes:
      - ${CACHEGRINDDIR}:/cachegrind:rw
      - ${SITESDIR}:/sites:rw
      - ${MYSQLSOCK}:/mysql.sock:rw
    expose:
      - "9000"