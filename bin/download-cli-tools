#!/bin/bash

FILES=./etc/cli-tools/*
for f in $FILES
do
  echo "Processing $f file..."
  # take action on each file. $f store current file name
  echo $(basename ${f%.*})!
  $(cat $f) > ./var/cli-tools/$(basename ${f%.*})
  chmod +x ./var/cli-tools/$(basename ${f%.*})
done